# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# Database
*.db
*.sqlite3

# ML artifacts (too large for git; rebuild — see README "Full ML training")
models/*.pkl
models/*.npz
models/*.npy
models/*.json
# Frozen eval splits (regenerable via build_wine_split.py)
models/wine_split/

# Data (download via data/download_foodcom.py)
data/RAW_recipes.csv
data/RAW_interactions.csv
data/openfoodfacts_cache.json

# Wine datasets (download via data/wine/download_wines.py)
data/wine/**/*.csv
data/wine/*.csv
# ...but keep the small committed sample so the wine demo seeds on a fresh clone
!data/wine/clean_wines.sample.csv

# Frontend
frontend/node_modules/
frontend/dist/
frontend/playwright-report/
frontend/test-results/
frontend/demo-video/

# Credentials (never commit)
kaggle.json
.kaggle/
*.key
*.pem

# Environment
.env
.env.local
.env.*.local
OPENAI_API_KEY

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Cursor's files
.cursor/